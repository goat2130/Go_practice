package main

import (
	"fmt"
	"strings"
)

func main() {
	var N int
	var S string
	fmt.Scanf("%d\n", &N)
	fmt.Scanf("%s\n", &S)

	count := 0
	for i := 1; i <= N-2; i++ {
		if strings.HasPrefix(S[i-1:], "#.#") {
			count++
		}
	}

	fmt.Println(count)
}
